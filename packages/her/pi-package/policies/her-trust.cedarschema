entity Agent;

entity Tool = {
  name: String,
  destructive: Bool,
  anchorPath?: Bool,
  allowedSelfModPath?: Bool,
};

action "CallTool" appliesTo {
  principal: Agent,
  resource: Tool,
  context: {},
};
