# 我们的设计系统(活文档)

> 宪法:**Make every detail perfect, and limit the number of details.**
> 本文档自身受它约束:层数不加、原则少而狠。目标只有一个——让她设计出最好的设计。
> 独有性的来源:Fei 的口味判例(design-taste/canon)× 我们的约束(中性黑白灰、说人话、真数据)× 下面这台合成器。散料进来,原理沉淀,表象丢弃。

## 系统骨架(五层,不再加层)

| 层 | 是什么 | 现状 |
|---|---|---|
| **原则** | 宪法一句 + canon 判例(限量是主张/可有可无=无/幸存者负全责) | 已立(knowledge/INDEX.md Core + design-taste/canon.md) |
| **流程** | 8 步 workflow(线框、定稿两硬门 + moodboard 轻门;薄出码) | 已批(docs/plan-workflow-redesign.md);W2 教材排队 |
| **空间** | 设计活在无限画布上:锁进/标尺/便签/手写标注;稿=可运行的 screen 文件 | 已内置她仓(packages/design-lab)+ design_lab_open 自动开 |
| **研究** | 看真实,不背菜谱:extract_design_md(任意站→观察值 DESIGN.md)+ 参考库正样本 | 工具已通;W-10 正样本库排队 |
| **语言** | 我们自己的 token/构图/密度语汇(说得出为什么,不是抄得像谁) | 首批构成技法已进(见台账 #5);仍最薄,继续喂 |

### 原则层判例(核心句的展开,源=台账 #5)

- **"细节"的定义比视觉宽**:每个 feature、控件、模式、状态、异常、行为,都是要有人设计、有人理解的细节。限量限的是这个总量,不只是装饰。
- **细节的成本是关系,不是个数**:两个按钮=两个按钮+它们的关系;细节涨,成本二次方涨。到某个数量你就不再是在设计细节,而是在管理细节——"Complexity is a tax on craft."
- **注意力预算不变量**:三个旋钮可以花六个月磨到极好;三十个就成了 enterprise software。三不比三十高贵——是注意力不随旋钮数扩容。
- **复杂度要挣门票**:747 驾驶舱配得上它的复杂,恒温器配不上。复杂不是罪,没挣到门票的复杂才是。
- **隐形决策禁令**(源=台账 #7):模型会"合理地、一致地、不声不响地"替你补上每个未做的决策——页面看着没事,规则正积累成没人选过的架构。决策要么显影记档,要么回退;"请求不是收据",落没落地看发布出来的那一帧。
- **加法有拥护者,删法有敌人**:800 个功能从来不是谁设计出来的,是"一次一个合理请求"长出来的——所以删除必须制度化(门禁/审计/幸存者负全责),不能靠自觉。

### 语言层构成技法(限量之下怎么"成"设计,源=台账 #5)

1. **复杂度集中**:问题真复杂时,别摊在表面——把交互聚进一个主控,把那个主控做到不可思议地好(一千首歌,一个轮子)。
2. **少数控件成为语言**:控件可以换岗,但意义要恒定(OP-1 四个旋钮凭颜色一致成为语汇)。学习被允许——乐器本来要学——但学习来自一致的语言,不来自标签和说明文案(这正是我们"禁解释性文字"的正面证明)。
3. **借已知心智模型**:最好的控件几乎不用解释,因为几乎无从误解(MUJI 拉绳=拉链灯)。想法小,比长得白难得多。
4. **高频动作要有恒定落点**:最常用的那五件,永远有个不动的家,不埋进玻璃后面(VW 认错:"It's a car, not a phone")。选出哪五件,是难的那半。

## 进料协议(每件来料过一遍,不许跳)

1. **真读完**(链接打开、视频看到帧、仓 clone 读源),禁摘要代原文。
2. **提炼原理,丢弃表象**:问"它真正携带的、可迁移的判断是什么"——不是它长什么样。
3. **定落层**:五层挑一;落不进任何层的,先入张力账,不硬塞。
4. **判决**:采(原样/改造)或拒,拒必写为什么——拒的理由和采的一样值钱。改造类判决用**四桶分类**(源=台账 #7 的适配算法):既定可复用模式(≥2 处一致且显然有意)→正式收编;有意的项目分歧→记为分歧并替换基线;一次性实现→**不编码**,顶多列开放问题;基线本就正确→不动。**关键告诫:被适配的项目自身也在受审**——它的某个不一致可能是 bug,把它正式化=把错误封圣;拿不准=问 Owner,不是立规则。
5. **张力显式记账**:与已有原则冲突的,不悄悄覆盖——摆出来,按宪法裁,裁不了留给 Fei。
6. **落成动作**(2026-08-31 Fei「所以?」立):每件采纳的料必须回答**"她的哪个动作因此变了"**——落进她设计时真加载的地方(knowledge/、门检、工具),写明落点。答不出=归档,不算采。本文档是账本,不是运行时;账本上的原理没接线就是收藏夹。

## 进料台账

| # | 来料 | 携带的原理(非表象) | 落层 | 判决 |
|---|---|---|---|---|
| 1 | timothymaarv interaction-lab | 设计稿应当是**活的**(可运行组件),评审发生在空间里而非文件列表里 | 空间 | 采,已建成 |
| 2 | ruler-mode / sticky-notes / label-maker | 标注是三种动词:量它/记它/指着它说话——工具跟着动词走,不堆功能 | 空间 | 采,已融合 |
| 3 | styles.refero.design | 设计参考要的是**真实站点的观察值**,不是二手风格词 | 研究 | 采,已转化为 extract_design_md |
| 4 | Fei 核心句(8/31) | 完美每一个细节,并限制细节的数量——限量先于打磨 | 原则 | 宪法,位阶最高 |
| 5 | Lightspark《Limit the number of details》(Geoff Teehan 2026-08-28,核心句的源文;句子本身出自 Jack Dorsey) | 前半句抓人,**后半句才是使前半句可能的那半**;细节=一切需被理解之物;成本在关系;注意力不扩容;复杂度挣门票;删法有敌人所以删除要制度化;以及三条构成技法(集中/语言/借用)+高频恒定落点 | 原则+语言 | 采,**已接线运行时**:knowledge/design/composition.md(新键,挂进「Any new design」必载行+新增「Deciding scope」行,桥热读免重启);另注:文章自身用活原型说话(可玩的 iPod/OP-1/CD 机),印证我们空间层"稿=可运行文件" |
| 7 | Matt Vidal《Figma→Claude→Webflow》+推文(2026-08-27) | 设计→实现之缝的治理:**设计文件是参考,项目系统是规格——冲突时系统赢设计弯**("用十二处是系统,用一处是一页");未做的决策会被模型静默补掉、悄悄积累成没人选过的架构→**决策必须显影**;"请求不是收据"——发布页是唯一证据(连自检量具也会错:他自己的溢出检查曾是假的);**每次收工归档所学**(症状追到机制才归档,"一修七页") | 流程+出码 | 采:①收工归档步接线 knowledge/process/filing.md;②出码教义进 plan W4;③四项准备=对既有件的独立印证(foundations/knowledge/W1a manifest)。**判决修正(supersede,带出处)**:初判"仓不 vendor"下于**只读文章未读仓**——被 Owner 一句"下载了吗"照出,违反进料协议第 1 步;全仓读毕后修正为**已 vendor**(references/figma-to-webflow-workflow,无 LICENSE 只许私用),因为仓里有三件文章没展开的通用资产:**PROJECT-HANDOFF 模板**(重写不追加/事实活验不抄前/显式改错认账/开局五分钟)→W2 handoff 教材基准件;**四桶适配算法**(05 文档)→已收进本协议第 4 步;**渲染页验证章**(scrollWidth 假绿三案例+假 bug 陷阱)→已接线 knowledge/review/rendered-page-verify(并合流我们自己的隐藏 pane 陷阱族);Webflow 专属机制(MCP 写坑/#w-node 特异性)仍不搬,类别保留。**skill 拆包追记(Owner「拆了吗能复用吗」)**:两个 .skill=zip,3,243 行全读。内容零复用(他们自己的判词:原样装=自信地套错架构);**形态三层全复用,定为基准件**:①双技能形态——"how to build"方法技能 + "what values"取值技能配对分离、永远同载(她未来的项目技能按此裁);②文件骨架——顶部速查表+带日期证据标签([verified]≠[documented])+supersede 留痕+带数字的失败案例+ID 注册表(每条带复采配方)+token 债"不许顺手修"+类预算(每节 5–8)+判决树+收官去向表("架构变更三件套同轮改,永不漂移");③通用判词——"写回声是部分状态,确认要用查询读"/"API 读有损时唯一证据是渲染物"/"受伤面比记录的窄,绕行前先探"/"修复工具不是乱建的执照"。落点:W2 教材基准件(与 handoff 模板并列);她的 skill-sharpen 采纳四桶+收官表=停车项待拍 |
| 6 | CasJam 推文(portable AI workflows) | 判断力要外置成可移植的文件(skills/文档),不锁进任何模型或 harness 的记忆——换模型换壳,系统不失忆 | 原则(元) | 采,收为系统自身的存在方式:本设计系统=一组文件(knowledge/canon/本文档/skills),任何读它的模型都按我们的方式设计;与 Her 北极星「own the memory, borrow the harness」同源 |
| 8 | Fei 重发 7 步工作流(2026-08-31 夜,归家判决后) | **工作流是宪法的时间形态**:结构先于细节、细节晚到且限量;线框是硬门因为它是"分歧最便宜的时刻"(灰盒改一个答案=一个灰盒,首稿后改=重画);composition 两问(挣门票/选五件)就问在灰盒上;知识随阶段载入——1–4 步只载 process/*,细节知识第 5 步起才进上下文(限量律管到她自己的加载) | 流程 | 采,落为 skill 教材 `references/process/steps.md`(Fable 亲写,W2 首篇)+ SKILL.md 首行任务;观察:此版 7 步无 to-code,已问 Owner——随后 Owner 重投 Vidal 出码节(行 9)= 第 8 步保留 |
| 9 | Fei 重投 Vidal「The project is the specification, Figma is the reference」节(2026-08-31 夜,点名"赶紧应用上,不是白给你看的") | 落码时权威翻转:目标项目(token/组件/命名)是规格,稿子只是参照——逐值找家、近 token 吸附(系统赢设计弯)、无 token 停下报债不许静默造值;偏离稿子=决策必须显影,含"感觉是改进"的那种;写请求不是收据,渲染出来的页才是证据 | 流程+出码 | 采,W4 教材落地 `references/process/to-code.md` + SKILL.md 加行 + steps.md 第 8 步挂链;**此前该教义只躺在 plan W4 一格里——记在计划≠接线运行时,Owner 一投照出的正是这条缝**;同时裁决 7-vs-8:Owner 主动推出码原则=第 8 步保留 |
| 10 | Victoria(@victoria_framer)"my new fav animation" 视频 + 引文 Framer Agents 官宣(2026-08-31 夜) | **镜头替 agent 表演注意力**:agent 改哪个元素,画布就飞进哪——帧证(8.4s,抽 2fps 亲看):0–4.5s 全景静置 → 4.5–5.5s 俯冲进 YORK wordmark、虚线选中、字形当众在变 → 6s 带动态模糊拉出 → 落定。人全程不丢空间上下文,"改了什么"不用自己扫 diff。引文=Framer Agents("built into every part…not replace you…help you ship")——「agent 长在画布里」被商业同行独立做出,印证我们空间层架构与 design_lab_open 反射弧 | 空间 | 采原理;落成动作=立卡 **G-380 edit spotlight**(她的 screen 编辑落地时,lab 相机飞向改动区+虚线短高亮+拉回,复用既有相机系统)——新功能属新范围,荐建、待 Owner 拍;引文部分=印证既有架构,无新动作 |

| 11 | tracepaper(caffeinum,MIT,`74d2756`,2026-09-05 读毕) | **一条回路做透 > 十个功能**:SPEC 明写 non-goals(不做 auth/presence/token/组件同步/代码导出),只做 draw→comment→read 一条。反馈通道的**语义**比它的 UI 重要:游标必须是**位置**不是记录 id——id 会被"解决/编辑"重新定位,拖着边界越过中间所有内容并永久丢失;空页要把原游标**回声**给调用方,返回 null 等于把人送回队首。`author` **服务端盖章**:被推上画布的内容可能来自不可信输入,它若能自称"人类"就成了给 agent 下指令的通路 | 空间+流程 | 采。落成动作:`design/canvas/feed.jsonl` 追加事件流 + 她的 `design_lab_notes`/`_reply`/`_resolve`——**她第一次有了耳朵**(此前 `design_lab_still` 只让她看见一个色块,读不到字、没法回)。我们把它的 `BEGIN IMMEDIATE` 换成**行位置即序号**:没人写序号就没人抢序号 |
| 12 | doop(kgoedecke,🔴 **AGPL-3.0,一行不许抄**,`86a42f1`,2026-09-05 读毕) | **最懂怎么伺候 agent**。四条可迁移判断:①**反馈搭车推送**(`withFeedback`:把待办人类反馈追加进**每一次**工具返回,附"先处理这个再继续你的计划")——一个 agent 会忘记调的工具等于一条不存在的通道;②指令工具明写"长对话压缩过就再调一次"(防上下文压缩把纪律吃掉);③**增量编辑**(`append_frame_html`/`edit_frame_html`)而非整篇替换,直接堵掉 tracepaper 自己承认的"没读回就推=静默丢弃设计";④决策→蒸馏→**只提案,人裁决才落**("Auto-committed memory goes noisy; curated memory stays trusted") | 流程+原则 | 采原理,独立重写。落成动作:G-427 搭车提醒 + 决策台账。**改进其消费语义**:doop 送达即消费(她忽略一次就永远消失),我们改为**按行动清除**——只挂"未解决且她没回过"的线程,读永远消不掉,只有回复或标结能。与工具描述里"resolved 是对工作的声明,不是对注意力的声明"同一条律 |
| 13 | onlook(Apache-2.0,`423e2e9`,2026-09-05 读毕) | **稿即真代码时,像素与源码必须能互相指认**——这是"设计即代码"的硬件前提。它的手段:AST 给每个 JSX 开标签注入 `data-oid`,建 `oid→{文件,起止行列,组件,是否在 map/条件里}`,点一下就回到精确源码区间 | 空间 | **采意图,拒手段**。拒的理由:它把 `data-oid` **永久写进源文件**,污染她的代码库;且它需要 oid 是为了**写回源码**,而我们只需要**指**。落成动作:2026-09-05 真浏览器实测——React 19 已删 `_debugSource`,但 fiber 的 `_debugStack` 直接给出 `at PlaygroundScreen (src/screens/playground/screen.tsx:19:25)`。**于是零注入、零依赖、零构建步、源码一个字不脏**(G-428)。同时拒其体量:30 个编辑器模块与"人推像素"的核心交互,与北极星(他退出循环)反向 |
| 14 | frameground(basta,MIT,`2a793cb`,2026-09-05 读毕) | 三条纪律:①**token 与 feel 分家**——DESIGN.md 装机器可读的 token,FEEL.md 装动效/构图/氛围的散文,因为后者本来就不该被塞进键值对;②**第一帧硬停**——DESIGN.md 还是 TODO 时不许静默替项目选一个审美方向,"第一帧会成为这个项目后续所有帧的法律";③**并行探索前的人工闸**——`/alternatives` 派子代理之前必须先让人确认那 N 个方向,它自己写明这是**防止并行子代理收敛的主要防线** | 语言+流程 | 采三条纪律。拒两样:①它的**帧模型**(自包含 HTML 快照,自己明写 "no round-trip back to the source app")——撞我们"稿=可运行的 screen 文件";②它的 **token 面板可以随手改出一套没人 ship 过的值**——撞 `design_system_load` 的铁律「这些是产品真正 ship 的值,不许发明」。**此层四家都不如我们:它们的 token 是宣称的,我们的是从上线 `globals.css` 按 commit 抽的、带收据** |

| 15 | lunagraph(闭源,`nicepkg/lunagraph` 已 404,官方文档+全部 guide,2026-09-06 读毕) | **设计是代码,但不是你的代码库**——代码库是当前真相,稿是未来真相,**两者之间的漂移是目的,不是要消灭的 bug**。跨源它没有解决,是**绕开了三次,每次手段不同**:①画布上跑的是**它自己托管的 React**(同源,所以点选/改组件/props 变旋钮/Cmd+L 引用全在这一层);②嵌你 localhost 的预览**故意只读只点**,要改得点 Edit **捞出一份可编辑副本**到它自己画布上——改的是副本,右边跟踪改了什么,Save to code 由**你机器上的 Claude Code** 写回(它不跑模型,吃你的 Pro/Max);③反方向靠 **Chrome 扩展**读外站 DOM——那是网页永远没有的宿主权限,开 Capture components 抓自己的站还能保住组件身份。另:每个 prop 自动成为画布控件;Edit component 改一处、全实例更新;prototype 分享链接;自带 MCP 供外部 agent 取页面 | 空间+流程 | **采两样**:①漂移教义(与我们「稿=会 ship 的 screen」不冲突——它管的是稿与产品**该不该**一致);②回程编辑的形状——**你编辑的永远是自己拥有的那份副本,手不伸进外来 iframe**(这条直接决定了我们不需要反代来做「读」)。**存疑**:它最值钱的两块恰恰 ship 得最少——「连接代码库」锁在发邮件申请后面(全仓模式还要 Tailwind v4 + React),「代码改动回流到设计并由人审批」只在 Concepts 里描述,**没有 guide 页**。**推断(未验证)**:第二条那份副本大概率靠它是 **macOS 桌面应用**(壳里的 webview 不受同源策略管);对我们的换算是——她本来就走 Playwright 进实验室,**CDP 能读跨源 iframe**,桌面壳给它的能力我们从 CDP 拿 |
| 16 | screenshot-to-code(abi,MIT,`d026163`,2026-09-06 读毕;Fei 投料) | 它已经从「一次 prompt」长成**带工具的 agent**(create_file/edit_file/extract_assets/screenshot_preview/generate_images/edit_images/remove_backgrounds/retrieve_option)。三条可迁移:①**要它看自己的活,就把像素塞进工具返回值本身**——screenshot_preview 返回的是 multimodal image parts 而不是路径,模型不管想不想看都看见了(「叫她记得去打开」是一句请求,不是一条通路);②**只抠资产,永不抠版式**——extract_assets 的提示词明写绝不把整张截图抠下来贴上去,否则得到的不是页面,是一张画;③**选中元素的 outerHTML 当定位符**,让模型按标签/类名/id/文本回源码里找——没有 AST 注入也没有 fiber,粗,但**在 fiber 够不着的地方(跨源的产品预览)是唯一还成立的那条**。另:评测是 16 张截图**人肉 1–4 分**、同组合跑三次取平均——**这个类别最出名的项目,至今判好坏仍靠人眼,没有自动裁判** | 流程+出码 | **①已落**:`design_lab_still` 改为把帧当 image parts 挂在返回值上,并附缩放坐标换算注记(帧要压到模型吃得下,而她是拿静帧量东西的)——commit `019412afc`。**③排队**:作为 fiber 定位的补集,给跨源预览用。**②待拍**:extract_assets 是我们纯粹没有的能力(「给已有产品重做 UI」的成败就在它),但要新供应商 + 真花钱,归 Fei。**拒两样**:它的输出形态(Tailwind/Babel 从 CDN 进一个单文件 HTML)撞我们「稿=会 ship 的 screen 文件」;它的 agent 引擎与 provider 层——她已经是 agent |

## 采样方针(Fei 2026-09-05 拍板)

> **机制上学 doop(它最懂怎么伺候 agent),形态上守 onlook 的立场但不要它的体量,纪律上抄 tracepaper 的克制,流程上取 frameground 的那道人工闸。**

四家收敛到同一形态(无限画布 + 帧 + sandbox iframe + agent 用工具写帧 + 人机同画布 + 全部放弃矢量编辑),这个形态不必再论证。**真正的选择在分歧处**:帧是死 HTML 还是真代码(我们站真代码,且比 onlook 更彻底——screen 会 ship);谁在编辑(我们站"她写、他评",因为北极星是他退出循环);反馈怎么回到 agent(搭车推送,不靠她记得);token 从哪来(从真实产品抽,此条不动)。

**2026-09-06 补(六家之后)**:方针不变,补两条分歧处的判词。**回程编辑**:lunagraph 与 onlook 领先的那一半,正确形状是「编辑自己拥有的副本」而不是「伸手进外来 iframe」——因此我们**不需要反代来做读这一侧**(她走 CDP)。**像素→源码**:四种答法(onlook 写 data-oid 进你的源码 / lunagraph 捞副本 / s2c 拿 outerHTML 当定位符 / 我们读 React 19 的 fiber),我们这条**零注入、零构建步、源码一个字不脏**,并且已经收成一条:全仓只留一个解析器,而先前 spotlight 那个**从未对着真实 React 渲染跑过**(commit `779b40ea0`)。

## 归属判决(Fei 2026-08-31 夜拍板)

**design mode 的家 = her(samantha 仓);brilliant-local 归位为独立产品 loora。** 理由:selfmod 射程(她只能磨自己仓里的功)、可移植性(判断力随身带)、产品边界(loora 是 Owner 自己的作品)。执行走**按挣门票渐迁**(Owner「这是最优解嘛?」逼出的修正——迁移也是复杂度,只搬已在真用的):
1. 即刻:通用知识 → 她仓 skill `her-design`(selfmod 白名单内);台账+地图归 her;**冻结线生效——本仓不再新增 her 器官,W2/W3 直接写她仓**
2. 她首次真用 research 时:extract_design_md 迁她工具
3. 毕业考前:8 步门随迁
4. loora 保留 tools/reference 与全部现有副本,两产品各自演化;分家后**本文件的活版本在她仓**,此份为存档

## 张力账(冲突不许静默解决)

(空——第一条冲突出现时记在这里,按宪法裁。)
