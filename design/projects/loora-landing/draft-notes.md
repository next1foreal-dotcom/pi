# loora landing — 首稿(draft)· step 5

2026-09-02 半稿入库 `9dc818ca2`；本文件 2026-09-03 补记。结构锁在线框门：页面即活画布，文案四角。本步只皮肤。

## 方向句(沿用 A · 画室石墨)

> 深石墨地面(产品 studio `#393d3c` 族:被盯几小时的工具面就地成为页面地面),稀疏密度(首屏几乎只有一块能拖能缩能落点的活画布,文案退成边上小字),安静车间 mood(上手就懂,不讲解),色姿 = 产品自身无彩石墨 + 唯一确认档(accent 只出现在「本机运行」与选中四角),reject:三张等卡、营销紫蓝渐变、Get Started、装饰粒子、把画布做成假截图。

## 相对线框加了什么 · 每一处为什么

| 加了什么 | 为什么 | 出处 |
|---|---|---|
| 地/面/线/文换成 studio 令牌(`#393d3c/#414544/#4a4e4d/#545857/#575c5a` 文三段 `#e5e6e6/#b4b7b6/#8b8f8d`) | 线框是中性灰盒;首稿地面必须是产品默认主题,不另造一套色 | research.md §0 |
| 无彩 accent `#dfe2e1`(fg `#2b2f2e`)只落在「本机运行」胶囊和选中四角 | 10% 确认档;整页无红绿蓝。选中态用同一枚,不另发明高亮 | 产品 token; moodboard A |
| radius 5/7/10 | 线框零半径是硬门纪律;进皮肤后用产品自己的圆角族,不调 | 产品 token |
| 字体 Merrion Sans(lab `/fonts/merrion-sans`) | 线框用系统脸。宪法禁 Inter;brief 垫过 Fraunces/Geist,**本稿改垫 lab 已有的 Merrion**,不引入新字体文件、不碰 Inter | lab 字体; **我垫的,可翻** |
| `--ease cubic-bezier(0.2,0,0,1)` · `--dur 120ms` · `--dur-2 190ms` 用在链接色和命令条背景 | 状态变化才动;无入场编排、无网格呼吸 | 产品 motion token |
| `prefers-reduced-motion` 把上述过渡打到 0.01ms | 信息不能活在运动里 | 产品 + research 三站均有 |
| 砍掉 frame/rect 上的 `frame`/`rect` 标签 | 线框家具,皮肤阶段还挂着就是在讲解节点类型。文字节点留下「真节点 · 能拖」,那是画布上唯一的句子 | composition 入场测试 |
| 命令条 `is-copied`(胶囊改 `--ll-float`,文案「已复制」) | 点了要看见状态落地,不是换一个装饰 | actions: 一个 primary |
| 字阶从线框的边注感抬到页面密度(词标 16/主张 14/提示 14,不再用工具面 11–13 当正文) | research 禁:「页面正文引用产品工具面的 11/12/13」 | research.md §0 禁 |

**没加的(诚实):** 本机没有跑着的 loora 本体。`localhost:5180` 是 design lab。`design_asset_shot` 没有可拍的产品 URL,所以没有真产品帧,也不拿假图充数。微画布证明形态(拖/缩/落),几何不是本体,没有 agent 流式落点。

**没动的:** 画布即页面;HUD 四角(词标 / 信任 / 源码 / 主张+提示 / 命令+本机运行);种子仍是 frame + 两 rect + 一文案节点——这一刀留给 step 6。

## 构成自检(对着 9/2 入库帧,不是对着代码)

- 焦点:zoom out 仍是中央那簇,不是标题。
- 脊柱:HUD 挂四角,中间让给画布。
- 密度:画布开、HUD 密而小。
- **失败的一处:** 簇里两块灰砖几乎等重(176×104 对 160×88)。缩远看是一对双胞胎,主从不成立。step 6 第一刀点名就是它。

## 我最不确定的三点

1. **两块等重灰砖** — 2026-09-03 已砍右砖,簇放大居中。裁决写在 project.json 该轮 iteration。
2. **微画布算不算见到 loora** — 形态对、产品未到。真本体帧做不到,等产品在这台机器上跑起来再换嵌。不拿截图框假装。
3. **Merrion 对 Fraunces/Geist** — brief 垫的是宪法那对;我改垫 lab 现成的 Merrion,因为这页是工具面不是编辑纸,衬线标题会把车间说成杂志。**我垫的,可翻。**

## 文案(未改,仍是线框门上的真文案)

- 词标:loora
- 主张:本地无限画布。agent 落真节点,你能拖。
- 信任:无账号 · 无服务器 · 无遥测
- 源码:github.com/next1foreal/brilliant-local(**垫的 URL,待定**)
- 提示:拖空白平移 · 滚轮缩放 · 点空白落一个矩形
- 命令:`npm install && npm run dev`
- 主按钮:本机运行 / 已复制
