#!/bin/sh

# The chain moved to .githooks/pre-commit , which is tracked and therefore exists
# in worktrees too -- see that file for why. This delegates so the hook still
# works while core.hooksPath is still .husky/_ .

exec sh .githooks/pre-commit "$@"
